/**
 * Auth redirect URL. Always production — never localhost.
 * Add this exact URL in Supabase: Authentication → URL Configuration → Redirect URLs.
 */
export const AUTH_CALLBACK_URL = "https://chat.gomarai.com/auth/callback";
